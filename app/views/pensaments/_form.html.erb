<%= form_with(model: pensament) do |form| %>
  <% if pensament.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(pensament.errors.count, "error") %> prohibited this pensament from being saved:</h2>

      <ul>
        <% pensament.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
    <%= form.label :photos, style: "display: block" %>
    <%= form.file_field :photos,:multiple => true %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
