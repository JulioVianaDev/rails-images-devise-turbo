<div id="<%= dom_id post %>">
  <p>
    <strong>Name:</strong>
    <%= post.name %>
    <%if post.image.attached?%>
    <strong>Image:</strong>
    <%= image_tag(post.image)%>
    <%end%>
  </p>

</div>
